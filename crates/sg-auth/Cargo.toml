[package]
name = "sg-auth"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "StreamGuard gateway authentication: HS256 session tickets (JWT)"

[features]
default = []
# Test-only resolver-backed credential provider. Production dependencies must
# not enable this feature.
test-credentials = []

[dependencies]
ring.workspace = true
rustls.workspace = true
sg-core.workspace = true
thiserror.workspace = true
